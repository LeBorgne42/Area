{% extends 'base.html.twig' %}

{% trans_default_domain 'front_report' %}

{% block title %}{{ 'title'|trans }}{% endblock %}

{% block body %}
    <div class="front-block display-message report">
        {% for report in reports %}
        <table class="table table-striped table-bordered table-dark">
            <tbody>
            <tr>
                <th class="tab-cells-name">
                    <span class="ml-3{% if report.getTitle() == 'Rapport de combat : Défaite' %} text-rouge{% elseif report.getTitle() == 'Rapport de combat : Victoire' %} text-vert{% endif %}">{{ report.getTitle() }}</span>
                    <span class="float-right">{{ report.getSendAt()|date('Y/m/d H:i:s') }}</span>
                </th>
            </tr>
            <tr>
                <th colspan="2" class="tab-cells-name">
                    <div class="p-3">
                        {{ report.getContent()|raw }}
                    </div>
                </th>
            </tr>
            </tbody>
        </table>
        {% endfor %}
    </div>
{% endblock %}
