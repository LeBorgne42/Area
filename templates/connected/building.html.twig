{% extends 'base.html.twig' %}

{% trans_default_domain 'front_building' %}

{% block title %}{{ 'title'|trans }}{% endblock %}

{% block body %}
    <div class="front-block">
        <div class="building-title"><h2>{{ 'building.titleRessource'|trans }}</h2></div>
        <table class="table table-striped table-bordered table-dark text-center">
            <tbody>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/mining.png') }}">{{ 'building.mining'|trans }} x {{ usePlanet.getBuilding().getMiner().getLevel() }}
                    <div class="mt-4">{{ usePlanet.getBuilding().getMiner().getNiobium() }} <img src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ usePlanet.getBuilding().getMiner().getWater() }} <img src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">{{ usePlanet.getBuilding().getMiner().getGround() }} <img src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells  p-4">
                    {% if usePlanet.getNiobium() >= usePlanet.getBuilding().getMiner().getNiobium() and usePlanet.getWater() >= usePlanet.getBuilding().getMiner().getWater() and usePlanet.getGroundPlace() + usePlanet.getBuilding().getMiner().getGround() <= usePlanet.getGround() %}
                        <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_mine', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getBuilding().getMiner().getLevel() > 1 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_mine', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/extract.png') }}">{{ 'building.extract'|trans }} x {{ usePlanet.getBuilding().getExtractor().getLevel() }}
                    <div class="mt-4">{{ usePlanet.getBuilding().getExtractor().getNiobium() }} <img src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ usePlanet.getBuilding().getExtractor().getWater() }} <img src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">{{ usePlanet.getBuilding().getExtractor().getGround() }} <img src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getNiobium() >= usePlanet.getBuilding().getExtractor().getNiobium() and usePlanet.getWater() >= usePlanet.getBuilding().getExtractor().getWater() and usePlanet.getGroundPlace() + usePlanet.getBuilding().getExtractor().getGround() <= usePlanet.getGround() %}
                    <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_extract', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getBuilding().getExtractor().getLevel() > 1 %}
                    <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_extract', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
        <div class="building-title"><h2>{{ 'building.titleEco'|trans }}</h2></div>
        <table class="table table-striped table-bordered table-dark text-center">
            <tbody>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/search.png') }}">{{ 'building.search'|trans }} x {{ usePlanet.getBuilding().getMiner().getLevel() }}
                    <div class="mt-4">{{ usePlanet.getBuilding().getMiner().getNiobium() }} <img src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ usePlanet.getBuilding().getMiner().getWater() }} <img src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">{{ usePlanet.getBuilding().getMiner().getGround() }} <img src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells  p-4">
                    {% if usePlanet.getNiobium() >= usePlanet.getBuilding().getMiner().getNiobium() and usePlanet.getWater() >= usePlanet.getBuilding().getMiner().getWater() and usePlanet.getGroundPlace() + usePlanet.getBuilding().getMiner().getGround() <= usePlanet.getGround() %}
                        <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_mine', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getBuilding().getMiner().getLevel() > 1 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_mine', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/city.png') }}">{{ 'building.city'|trans }} x {{ usePlanet.getBuilding().getMiner().getLevel() }}
                    <div class="mt-4">{{ usePlanet.getBuilding().getMiner().getNiobium() }} <img src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ usePlanet.getBuilding().getMiner().getWater() }} <img src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">{{ usePlanet.getBuilding().getMiner().getGround() }} <img src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells  p-4">
                    {% if usePlanet.getNiobium() >= usePlanet.getBuilding().getMiner().getNiobium() and usePlanet.getWater() >= usePlanet.getBuilding().getMiner().getWater() and usePlanet.getGroundPlace() + usePlanet.getBuilding().getMiner().getGround() <= usePlanet.getGround() %}
                        <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_mine', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getBuilding().getMiner().getLevel() > 1 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_mine', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/metropole.png') }}">{{ 'building.metropole'|trans }} x {{ usePlanet.getBuilding().getExtractor().getLevel() }}
                    <div class="mt-4">{{ usePlanet.getBuilding().getExtractor().getNiobium() }} <img src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ usePlanet.getBuilding().getExtractor().getWater() }} <img src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">{{ usePlanet.getBuilding().getExtractor().getGround() }} <img src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getNiobium() >= usePlanet.getBuilding().getExtractor().getNiobium() and usePlanet.getWater() >= usePlanet.getBuilding().getExtractor().getWater() and usePlanet.getGroundPlace() + usePlanet.getBuilding().getExtractor().getGround() <= usePlanet.getGround() %}
                        <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_extract', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getBuilding().getExtractor().getLevel() > 1 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_extract', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
        <div class="building-title mt-3"><h2>{{ 'building.titleShip'|trans }}</h2></div>
        <table class="table table-striped table-bordered table-dark text-center">
            <tbody>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/caserne.png') }}">{{ 'building.caserne'|trans }} x {{ usePlanet.getBuilding().getMiner().getLevel() }}
                    <div class="mt-4">{{ usePlanet.getBuilding().getMiner().getNiobium() }} <img src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ usePlanet.getBuilding().getMiner().getWater() }} <img src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">{{ usePlanet.getBuilding().getMiner().getGround() }} <img src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells  p-4">
                    {% if usePlanet.getNiobium() >= usePlanet.getBuilding().getMiner().getNiobium() and usePlanet.getWater() >= usePlanet.getBuilding().getMiner().getWater() and usePlanet.getGroundPlace() + usePlanet.getBuilding().getMiner().getGround() <= usePlanet.getGround() %}
                        <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_mine', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getBuilding().getMiner().getLevel() > 1 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_mine', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/chantier.png') }}">{{ 'building.chantier'|trans }} x {{ usePlanet.getBuilding().getSpaceShip().getLevel() }}
                    <div class="mt-4">{{ usePlanet.getBuilding().getSpaceShip().getNiobium() }} <img src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ usePlanet.getBuilding().getSpaceShip().getWater() }} <img src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">{{ usePlanet.getBuilding().getSpaceShip().getGround() }} <img src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells  p-4">
                    {% if usePlanet.getNiobium() >= usePlanet.getBuilding().getSpaceShip().getNiobium() and usePlanet.getWater() >= usePlanet.getBuilding().getSpaceShip().getWater() and usePlanet.getGroundPlace() + usePlanet.getBuilding().getSpaceShip().getGround() <= usePlanet.getGround() %}
                        <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_spaceShipyard', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getBuilding().getSpaceShip().getLevel() > 1 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_spaceShipyard', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/light.png') }}">{{ 'building.light'|trans }} x {{ usePlanet.getBuilding().getMiner().getLevel() }}
                    <div class="mt-4">{{ usePlanet.getBuilding().getMiner().getNiobium() }} <img src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ usePlanet.getBuilding().getMiner().getWater() }} <img src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">{{ usePlanet.getBuilding().getMiner().getGround() }} <img src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells  p-4">
                    {% if usePlanet.getNiobium() >= usePlanet.getBuilding().getMiner().getNiobium() and usePlanet.getWater() >= usePlanet.getBuilding().getMiner().getWater() and usePlanet.getGroundPlace() + usePlanet.getBuilding().getMiner().getGround() <= usePlanet.getGround() %}
                        <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_mine', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getBuilding().getMiner().getLevel() > 1 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_mine', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/heavy.png') }}">{{ 'building.heavy'|trans }} x {{ usePlanet.getBuilding().getExtractor().getLevel() }}
                    <div class="mt-4">{{ usePlanet.getBuilding().getExtractor().getNiobium() }} <img src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ usePlanet.getBuilding().getExtractor().getWater() }} <img src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">{{ usePlanet.getBuilding().getExtractor().getGround() }} <img src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getNiobium() >= usePlanet.getBuilding().getExtractor().getNiobium() and usePlanet.getWater() >= usePlanet.getBuilding().getExtractor().getWater() and usePlanet.getGroundPlace() + usePlanet.getBuilding().getExtractor().getGround() <= usePlanet.getGround() %}
                        <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_extract', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getBuilding().getExtractor().getLevel() > 1 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_extract', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
        <div class="building-title"><h2>{{ 'building.titleSpy'|trans }}</h2></div>
        <table class="table table-striped table-bordered table-dark text-center">
            <tbody>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/radar.png') }}">{{ 'building.radar'|trans }} x {{ usePlanet.getBuilding().getMiner().getLevel() }}
                    <div class="mt-4">{{ usePlanet.getBuilding().getMiner().getNiobium() }} <img src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ usePlanet.getBuilding().getMiner().getWater() }} <img src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">{{ usePlanet.getBuilding().getMiner().getGround() }} <img src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells  p-4">
                    {% if usePlanet.getNiobium() >= usePlanet.getBuilding().getMiner().getNiobium() and usePlanet.getWater() >= usePlanet.getBuilding().getMiner().getWater() and usePlanet.getGroundPlace() + usePlanet.getBuilding().getMiner().getGround() <= usePlanet.getGround() %}
                        <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_mine', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getBuilding().getMiner().getLevel() > 1 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_mine', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/skyRadar.png') }}">{{ 'building.skyRadar'|trans }} x {{ usePlanet.getBuilding().getExtractor().getLevel() }}
                    <div class="mt-4">{{ usePlanet.getBuilding().getExtractor().getNiobium() }} <img src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ usePlanet.getBuilding().getExtractor().getWater() }} <img src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">{{ usePlanet.getBuilding().getExtractor().getGround() }} <img src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getNiobium() >= usePlanet.getBuilding().getExtractor().getNiobium() and usePlanet.getWater() >= usePlanet.getBuilding().getExtractor().getWater() and usePlanet.getGroundPlace() + usePlanet.getBuilding().getExtractor().getGround() <= usePlanet.getGround() %}
                        <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_extract', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getBuilding().getExtractor().getLevel() > 1 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_extract', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/brouilleur.png') }}">{{ 'building.brouilleur'|trans }} x {{ usePlanet.getBuilding().getMiner().getLevel() }}
                    <div class="mt-4">{{ usePlanet.getBuilding().getMiner().getNiobium() }} <img src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ usePlanet.getBuilding().getMiner().getWater() }} <img src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">{{ usePlanet.getBuilding().getMiner().getGround() }} <img src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells  p-4">
                    {% if usePlanet.getNiobium() >= usePlanet.getBuilding().getMiner().getNiobium() and usePlanet.getWater() >= usePlanet.getBuilding().getMiner().getWater() and usePlanet.getGroundPlace() + usePlanet.getBuilding().getMiner().getGround() <= usePlanet.getGround() %}
                        <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_mine', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getBuilding().getMiner().getLevel() > 1 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_mine', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
    </div>
{% endblock %}
