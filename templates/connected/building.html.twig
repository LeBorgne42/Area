{% extends 'base.html.twig' %}

{% trans_default_domain 'front_building' %}

{% block title %}{{ 'title'|trans }}{% endblock %}

{% block body %}
    <div class="front-block building">
        <div id="timeDefaut" hidden>{{ date|date('Y/m/d H:i:s') }}</div>
        <div class="block-title"><h2>{{ 'building.titleRessource'|trans }}</h2></div>
        <table class="table table-striped table-bordered table-dark text-center">
            <tbody>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/mining.png') }}">{{ 'building.mining'|trans }} x {{ usePlanet.getMiner()|trans }}
                    <div class="mt-4">{{ (usePlanet.getMiner() + 1) * 300 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'niobiumImg'|trans }}" src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ (usePlanet.getMiner() + 1) * 100 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'waterImg'|trans }}" src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">1 <img data-toggle="tooltip" data-placement="bottom" title="{{ 'groundImg'|trans }}" src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells  p-4">
                    {% if usePlanet.getConstruct() == 'miner' %}
                        <div id="timeConstruct" hidden>{{ usePlanet.getConstructAt()|date('Y/m/d H:i:s') }}</div>
                        <div id="timeDisplay" class="mt-5"></div>
                    {% else %}
                        {% if usePlanet.getNiobium() >= ((usePlanet.getMiner() + 1) * 300) and usePlanet.getWater() >= ((usePlanet.getMiner() + 1) * 100) and usePlanet.getGroundPlace() + 1 <= usePlanet.getGround() and usePlanet.getConstructAt() < date %}
                            <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_mine', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                        {% else %}
                            <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                        {% endif %}
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getMiner() > 1 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_mine', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/extract.png') }}">{{ 'building.extract'|trans }} x {{ usePlanet.getExtractor()|trans }}
                    <div class="mt-4">{{ (usePlanet.getExtractor() + 1) * 100 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'niobiumImg'|trans }}" src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ (usePlanet.getExtractor() + 1) * 300 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'waterImg'|trans }}" src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">1 <img data-toggle="tooltip" data-placement="bottom" title="{{ 'groundImg'|trans }}" src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getConstruct() == 'extractor' %}
                        <div id="timeConstruct" hidden>{{ usePlanet.getConstructAt()|date('Y/m/d H:i:s') }}</div>
                        <div id="timeDisplay" class="mt-5"></div>
                    {% else %}
                        {% if usePlanet.getNiobium() >= ((usePlanet.getExtractor() + 1) * 100) and usePlanet.getWater() >= ((usePlanet.getExtractor() + 1) * 300) and usePlanet.getGroundPlace() + 1 <= usePlanet.getGround() and usePlanet.getConstructAt() < date %}
                        <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_extract', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                        {% else %}
                            <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                        {% endif %}
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getExtractor() > 1 %}
                    <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_extract', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
        <div class="block-title"><h2>{{ 'building.titleEco'|trans }}</h2></div>
        <table class="table table-striped table-bordered table-dark text-center">
            <tbody>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/search.png') }}">{{ 'building.search'|trans }} x {{ usePlanet.getCenterSearch()|trans }}
                    <div class="mt-4">{{ (usePlanet.getCenterSearch() + 1) * 2850 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'niobiumImg'|trans }}" src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ (usePlanet.getCenterSearch() + 1) * 3580 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'waterImg'|trans }}" src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">5 <img data-toggle="tooltip" data-placement="bottom" title="{{ 'groundImg'|trans }}" src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells  p-4">
                    {% if usePlanet.getConstruct() == 'centerSearch' %}
                        <div id="timeConstruct" hidden>{{ usePlanet.getConstructAt()|date('Y/m/d H:i:s') }}</div>
                        <div id="timeDisplay" class="mt-5"></div>
                    {% else %}
                        {% if usePlanet.getNiobium() >= ((usePlanet.getCenterSearch() + 1) * 2850) and usePlanet.getWater() >= ((usePlanet.getCenterSearch() + 1) * 3580) and usePlanet.getGroundPlace() + 5 <= usePlanet.getGround() and usePlanet.getConstructAt() < date %}
                            <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_search', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                        {% else %}
                            <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                        {% endif %}
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getCenterSearch() > 0 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_search', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/city.png') }}">{{ 'building.city'|trans }} x {{ usePlanet.getCity()|trans }}
                    <div class="mt-4">{{ (usePlanet.getCity() + 1) * 15000 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'niobiumImg'|trans }}" src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ (usePlanet.getCity() + 1) * 11000 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'waterImg'|trans }}" src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">6 <img data-toggle="tooltip" data-placement="bottom" title="{{ 'groundImg'|trans }}" src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells  p-4">
                    {% if usePlanet.getConstruct() == 'city' %}
                        <div id="timeConstruct" hidden>{{ usePlanet.getConstructAt()|date('Y/m/d H:i:s') }}</div>
                        <div id="timeDisplay" class="mt-5"></div>
                    {% else %}
                        {% if usePlanet.getNiobium() >= ((usePlanet.getCity() + 1) * 15000) and usePlanet.getWater() >= ((usePlanet.getCity() + 1) * 11000) and usePlanet.getGroundPlace() + 6 <= usePlanet.getGround() and app.user.getDemography() > 0 and usePlanet.getConstructAt() < date %}
                            <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_city', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                        {% else %}
                            <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                        {% endif %}
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getCity() > 0 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_city', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/metropole.png') }}">{{ 'building.metropole'|trans }} x {{ usePlanet.getMetropole()|trans }}
                    <div class="mt-4">{{ (usePlanet.getMetropole() + 1) * 750000 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'niobiumImg'|trans }}" src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ (usePlanet.getMetropole() + 1) * 55000 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'waterImg'|trans }}" src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">8 <img data-toggle="tooltip" data-placement="bottom" title="{{ 'groundImg'|trans }}" src="{{ asset('img/ground.png') }}"><span class="ml-4">8 <img data-toggle="tooltip" data-placement="bottom" title="{{ 'skyImg'|trans }}" src="{{ asset('img/sky.png') }}"></span></div>
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getConstruct() == 'metropole' %}
                        <div id="timeConstruct" hidden>{{ usePlanet.getConstructAt()|date('Y/m/d H:i:s') }}</div>
                        <div id="timeDisplay" class="mt-5"></div>
                    {% else %}
                        {% if usePlanet.getNiobium() >= ((usePlanet.getMetropole() + 1) * 750000) and usePlanet.getWater() >= ((usePlanet.getMetropole() + 1) * 55000) and usePlanet.getGroundPlace() + 8 <= usePlanet.getGround() and usePlanet.getSkyPlace() + 8 <= usePlanet.getSky() and app.user.getDemography() == 5 and usePlanet.getConstructAt() < date %}
                            <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_metropole', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                        {% else %}
                            <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                        {% endif %}
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getMetropole() > 0 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_metropole', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
        <div class="block-title mt-3"><h2>{{ 'building.titleShip'|trans }}</h2></div>
        <table class="table table-striped table-bordered table-dark text-center">
            <tbody>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/caserne.png') }}">{{ 'building.caserne'|trans }} x {{ usePlanet.getCaserne()|trans }}
                    <div class="mt-4">{{ (usePlanet.getCaserne() + 1) * 5000 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'niobiumImg'|trans }}" src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ (usePlanet.getCaserne() + 1) * 750 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'waterImg'|trans }}" src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">6 <img data-toggle="tooltip" data-placement="bottom" title="{{ 'groundImg'|trans }}" src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells  p-4">
                    {% if usePlanet.getConstruct() == 'caserne' %}
                        <div id="timeConstruct" hidden>{{ usePlanet.getConstructAt()|date('Y/m/d H:i:s') }}</div>
                        <div id="timeDisplay" class="mt-5"></div>
                    {% else %}
                        {% if usePlanet.getNiobium() >= ((usePlanet.getCaserne() + 1) * 5000) and usePlanet.getWater() >= ((usePlanet.getCaserne() + 1) * 750) and usePlanet.getGroundPlace() + 6 <= usePlanet.getGround() and app.user.getDiscipline() == 3 and usePlanet.getConstructAt() < date %}
                            <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_caserne', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                        {% else %}
                            <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                        {% endif %}
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getCaserne() > 0 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_caserne', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/chantier.png') }}">{{ 'building.chantier'|trans }} x {{ usePlanet.getSpaceShip()|trans }}
                    <div class="mt-4">{{ (usePlanet.getSpaceShip() + 1) * 3000 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'niobiumImg'|trans }}" src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ (usePlanet.getSpaceShip() + 1) * 2000 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'waterImg'|trans }}" src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">10 <img data-toggle="tooltip" data-placement="bottom" title="{{ 'groundImg'|trans }}" src="{{ asset('img/ground.png') }}"><span class="ml-4">4 <img data-toggle="tooltip" data-placement="bottom" title="{{ 'skyImg'|trans }}" src="{{ asset('img/sky.png') }}"></span></div>
                </td>
                <td class="center-cells  p-4">
                    {% if usePlanet.getConstruct() == 'spaceShip' %}
                        <div id="timeConstruct" hidden>{{ usePlanet.getConstructAt()|date('Y/m/d H:i:s') }}</div>
                        <div id="timeDisplay" class="mt-5"></div>
                    {% else %}
                        {% if usePlanet.getNiobium() >= ((usePlanet.getSpaceShip() + 1) * 3000) and usePlanet.getWater() >= ((usePlanet.getSpaceShip() + 1) * 2000) and usePlanet.getGroundPlace() + 10 <= usePlanet.getGround() and usePlanet.getSkyPlace() + 4 <= usePlanet.getSky() and app.user.getIndustry() > 0 and usePlanet.getConstructAt() < date %}
                            <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_spaceShipyard', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                        {% else %}
                            <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                        {% endif %}
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getSpaceShip() > 0 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_spaceShipyard', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/light.png') }}">{{ 'building.light'|trans }} {% if usePlanet.getLightUsine() == 0 %}{{ 'building.canBe'|trans }}{% else %}{{ 'building.Activated'|trans }}{% endif %}
                    <div class="mt-4">{{ (usePlanet.getLightUsine() + 1) * 7000 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'niobiumImg'|trans }}" src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ (usePlanet.getLightUsine() + 1) * 3900 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'waterImg'|trans }}" src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">6 <img data-toggle="tooltip" data-placement="bottom" title="{{ 'groundImg'|trans }}" src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells  p-4">
                    {% if usePlanet.getConstruct() == 'lightUsine' %}
                        <div id="timeConstruct" hidden>{{ usePlanet.getConstructAt()|date('Y/m/d H:i:s') }}</div>
                        <div id="timeDisplay" class="mt-5"></div>
                    {% else %}
                        {% if usePlanet.getNiobium() >= ((usePlanet.getLightUsine() + 1) * 7000) and usePlanet.getWater() >= ((usePlanet.getLightUsine() + 1) * 3900) and usePlanet.getGroundPlace() + 6 <= usePlanet.getGround() and usePlanet.getLightUsine() != 1 and app.user.getLightShip() > 0 and usePlanet.getConstructAt() < date %}
                            <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_lightUsine', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                        {% else %}
                            <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                        {% endif %}
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getLightUsine() > 0 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_lightUsine', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/heavy.png') }}">{{ 'building.heavy'|trans }} {% if usePlanet.getHeavyUsine() == 0 %}{{ 'building.canBe'|trans }}{% else %}{{ 'building.Activated'|trans }}{% endif %}
                    <div class="mt-4">{{ (usePlanet.getHeavyUsine() + 1) * 83000 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'niobiumImg'|trans }}" src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ (usePlanet.getHeavyUsine() + 1) * 68000 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'waterImg'|trans }}" src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">12 <img data-toggle="tooltip" data-placement="bottom" title="{{ 'groundImg'|trans }}" src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getConstruct() == 'heavyUsine' %}
                        <div id="timeConstruct" hidden>{{ usePlanet.getConstructAt()|date('Y/m/d H:i:s') }}</div>
                        <div id="timeDisplay" class="mt-5"></div>
                    {% else %}
                        {% if usePlanet.getNiobium() >= ((usePlanet.getHeavyUsine() + 1) * 83000) and usePlanet.getWater() >= ((usePlanet.getHeavyUsine() + 1) * 68000) and usePlanet.getGroundPlace() + 12 <= usePlanet.getGround() and usePlanet.getHeavyUsine() != 1 and app.user.getHeavyShip() > 0 and usePlanet.getConstructAt() < date %}
                            <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_heavyUsine', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                        {% else %}
                            <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                        {% endif %}
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getHeavyUsine() > 0 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_heavyUsine', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
        <div class="block-title"><h2>{{ 'building.titleSpy'|trans }}</h2></div>
        <table class="table table-striped table-bordered table-dark text-center">
            <tbody>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/radar.png') }}">{{ 'building.radar'|trans }} x {{ usePlanet.getRadar()|trans }}
                    <div class="mt-4">{{ (usePlanet.getRadar() + 1) * 1200 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'niobiumImg'|trans }}" src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ (usePlanet.getRadar() + 1) * 650 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'waterImg'|trans }}" src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">2 <img data-toggle="tooltip" data-placement="bottom" title="{{ 'groundImg'|trans }}" src="{{ asset('img/ground.png') }}"></div>
                </td>
                <td class="center-cells  p-4">
                    {% if usePlanet.getConstruct() == 'radar' %}
                        <div id="timeConstruct" hidden>{{ usePlanet.getConstructAt()|date('Y/m/d H:i:s') }}</div>
                        <div id="timeDisplay" class="mt-5"></div>
                    {% else %}
                        {% if usePlanet.getNiobium() >= ((usePlanet.getRadar() + 1) * 1200) and usePlanet.getWater() >= ((usePlanet.getRadar() + 1) * 650) and usePlanet.getGroundPlace() + 2 <= usePlanet.getGround() and app.user.getOnde() > 0 and usePlanet.getConstructAt() < date %}
                            <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_radar', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                        {% else %}
                            <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                        {% endif %}
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getRadar() > 0 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_radar', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/skyRadar.png') }}">{{ 'building.skyRadar'|trans }} x {{ usePlanet.getSkyRadar()|trans }}
                    <div class="mt-4">{{ (usePlanet.getSkyRadar() + 1) * 20000 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'niobiumImg'|trans }}" src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ (usePlanet.getSkyRadar() + 1) * 17200 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'waterImg'|trans }}" src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">2 <img data-toggle="tooltip" data-placement="bottom" title="{{ 'skyImg'|trans }}" src="{{ asset('img/sky.png') }}"></div>
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getConstruct() == 'skyRadar' %}
                        <div id="timeConstruct" hidden>{{ usePlanet.getConstructAt()|date('Y/m/d H:i:s') }}</div>
                        <div id="timeDisplay" class="mt-5"></div>
                    {% else %}
                        {% if usePlanet.getNiobium() >= ((usePlanet.getSkyRadar() + 1) * 20000) and usePlanet.getWater() >= ((usePlanet.getSkyRadar() + 1) * 17200) and usePlanet.getSkyPlace() + 2 <= usePlanet.getSky() and app.user.getOnde() > 2 and usePlanet.getConstructAt() < date %}
                            <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_skyRadar', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                        {% else %}
                            <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                        {% endif %}
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getSkyRadar() > 0 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_skyRadar', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="center-cells">
                    <img class="float-left building-img ml-3" src="{{ asset('img/brouilleur.png') }}">{{ 'building.brouilleur'|trans }} x {{ usePlanet.getSkyBrouilleur()|trans }}
                    <div class="mt-4">{{ (usePlanet.getSkyBrouilleur() + 1) * 51000 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'niobiumImg'|trans }}" src="{{ asset('img/niobium.png') }}"><span class="ml-4">{{ (usePlanet.getSkyBrouilleur() + 1) * 32100 }} <img data-toggle="tooltip" data-placement="bottom" title="{{ 'waterImg'|trans }}" src="{{ asset('img/water.png') }}"></span></div>
                    <div class="mt-4">4 <img data-toggle="tooltip" data-placement="bottom" title="{{ 'skyImg'|trans }}" src="{{ asset('img/sky.png') }}"></div>
                </td>
                <td class="center-cells  p-4">
                    {% if usePlanet.getConstruct() == 'skyBrouilleur' %}
                        <div id="timeConstruct" hidden>{{ usePlanet.getConstructAt()|date('Y/m/d H:i:s') }}</div>
                        <div id="timeDisplay" class="mt-5"></div>
                    {% else %}
                        {% if usePlanet.getNiobium() >= ((usePlanet.getSkyBrouilleur() + 1) * 51000) and usePlanet.getWater() >= ((usePlanet.getSkyBrouilleur() + 1) * 32100) and usePlanet.getSkyPlace() + 4 <= usePlanet.getSky() and app.user.getOnde() == 5 and usePlanet.getConstructAt() < date %}
                            <a class="btn btn-lg btn-info mt-5" href="{{ path('building_add_brouilleur', {'idp': usePlanet.getId()}) }}">{{ 'building.build'|trans }}</a>
                        {% else %}
                            <a class="btn btn-lg btn-info mt-5" disabled>{{ 'building.build'|trans }}</a>
                        {% endif %}
                    {% endif %}
                </td>
                <td class="center-cells p-4">
                    {% if usePlanet.getSkyBrouilleur() > 0 %}
                        <a class="btn btn-lg btn-danger mt-5" href="{{ path('building_remove_brouilleur', {'idp': usePlanet.getId()}) }}">{{ 'building.demolish'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-danger mt-5" disabled>{{ 'building.demolish'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
    </div>
{% endblock %}
