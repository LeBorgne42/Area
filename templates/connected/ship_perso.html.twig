{% extends 'base.html.twig' %}

{% trans_default_domain 'front_ship_perso' %}

{% block title %}{{ 'title'|trans }}{% endblock %}

{% block body %}
    {% for message in app.flashes('fail') %}
        <div class="flash-notice mt-5 alert-flash-error text-center">
            {{ message }}
        </div>
    {% endfor %}
    <div class="front-block">
        {% if app.user.getTutorial() == 19 or app.user.getTutorial() == 20 %}
            <table class="table table-striped borderOverview tutorial">
                <tbody>
                <tr>
                    <th class="tab-cells-name p-3 tutorial">
                        <div class="row">
                            <div class="col-12 py-1">
                                {% if app.user.getTutorial() == 19 %}{{ 'quest.tutorial'|trans|raw }}{% elseif app.user.getTutorial() == 20  %}{{ 'quest.tutorialBis'|trans|raw }}{% endif %}
                            </div>
                        </div>
                    </th>
                </tr>
                </tbody>
            </table>
        {% endif %}
        <div class="front-block-second">
            {% set shipPoints = [commander.getShip().getArmorPoints() - 5115, commander.getShip().getShieldPoints() - 159, commander.getShip().getMissilePoints() - 272, commander.getShip().getLaserPoints() - 30, commander.getShip().getPlasmaPoints() - 25, commander.getShip().getPrecisionPoints()] %}
            <div id="shipPoints" data-ship-points="{{ shipPoints|json_encode }}"></div>
            <div class="row">
                <div class="col-12">
                    <div class="block-title mb-3"><h2>{{ 'ship.personalisation'|trans }}</h2></div>
                    <table class="table table-striped table-bordered table-dark persoFleetClick">
                        <tbody>
                        <tr>
                            <th width="74" class="p-0 hunterClick">
                                <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/hunter.webp') }}">
                            </th>
                            <td class="text-center hunterClick{% if app.user.getTutorial() == 19 %} tutorial-show{% endif %}">
                                {{ 'ship.hunter'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="{% if commander.getShip().getPointHunter() > 0 %}text-vert{% else %}text-rouge{% endif %}">{{ commander.getShip().getPointHunter() }}</span>
                            </td>
                            <th width="74" class="p-0 hunterHeavyClick">
                                <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/hunterHeavy.webp') }}">
                            </th>
                            <td class="text-center hunterHeavyClick">
                                {{ 'ship.hunterHeavy'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="{% if commander.getShip().getPointHunterHeavy() > 0 %}text-vert{% else %}text-rouge{% endif %}">{{ commander.getShip().getPointHunterHeavy() }}</span>
                            </td>
                            <th width="74" class="p-0 hunterWarClick">
                                <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/hunterWar.webp') }}">
                            </th>
                            <td class="text-center hunterWarClick">
                                {{ 'ship.hunterWar'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="{% if commander.getShip().getPointHunterWar() > 0 %}text-vert{% else %}text-rouge{% endif %}">{{ commander.getShip().getPointHunterWar() }}</span>
                            </td>
                            <th width="74" class="p-0 corvetClick">
                                <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/corvet.webp') }}">
                            </th>
                            <td class="text-center corvetClick">
                                {{ 'ship.corvet'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="{% if commander.getShip().getPointCorvet() > 0 %}text-vert{% else %}text-rouge{% endif %}">{{ commander.getShip().getPointCorvet() }}</span>
                            </td>
                        </tr>
                        <tr>
                            <th width="74" class="p-0 corvetLaserClick">
                                <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/corvetLaser.webp') }}">
                            </th>
                            <td class="text-center corvetLaserClick">
                                {{ 'ship.corvetLaser'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="{% if commander.getShip().getPointCorvetLaser() > 0 %}text-vert{% else %}text-rouge{% endif %}">{{ commander.getShip().getPointCorvetLaser() }}</span>
                            </td>
                            <th width="74" class="p-0 corvetWarClick">
                                <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/corvetWar.webp') }}">
                            </th>
                            <td class="text-center corvetWarClick">
                                {{ 'ship.corvetWar'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="{% if commander.getShip().getPointCorvetWar() > 0 %}text-vert{% else %}text-rouge{% endif %}">{{ commander.getShip().getPointCorvetWar() }}</span>
                            </td>
                            <th width="74" class="p-0 fregateClick">
                                <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/fregate.webp') }}">
                            </th>
                            <td class="text-center fregateClick">
                                {{ 'ship.fregate'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="{% if commander.getShip().getPointFregate() > 0 %}text-vert{% else %}text-rouge{% endif %}">{{ commander.getShip().getPointFregate() }}</span>
                            </td>
                            <th width="74" class="p-0 fregatePlasmaClick">
                                <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/fregatePlasma.webp') }}">
                            </th>
                            <td class="text-center fregatePlasmaClick">
                                {{ 'ship.fregatePlasma'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="{% if commander.getShip().getPointFregatePlasma() > 0 %}text-vert{% else %}text-rouge{% endif %}">{{ commander.getShip().getPointFregatePlasma() }}</span>
                            </td>
                        </tr>
                        <tr>
                            <th width="74" class="p-0 croiserClick">
                                <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/croiser.webp') }}">
                            </th>
                            <td class="text-center croiserClick">
                                {{ 'ship.croiser'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="{% if commander.getShip().getPointCroiser() > 0 %}text-vert{% else %}text-rouge{% endif %}">{{ commander.getShip().getPointCroiser() }}</span>
                            </td>
                            <th width="74" class="p-0 ironCladClick">
                                <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/ironClad.webp') }}">
                            </th>
                            <td class="text-center ironCladClick">
                                {{ 'ship.ironClad'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="{% if commander.getShip().getPointIronClad() > 0 %}text-vert{% else %}text-rouge{% endif %}">{{ commander.getShip().getPointIronClad() }}</span>
                            </td>
                            <th width="74" class="p-0 destroyerClick">
                                <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/destroyer.webp') }}">
                            </th>
                            <td class="text-center destroyerClick">
                                {{ 'ship.destroyer'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="{% if commander.getShip().getPointDestroyer() > 0 %}text-vert{% else %}text-rouge{% endif %}">{{ commander.getShip().getPointDestroyer() }}</span>
                            </td>
                        </tr>
                        </tbody>
                    </table>
                    <table class="table table-striped table-bordered table-dark persoFleet">
                        <tbody>
                        {% if commander.getShip().getPointHunter() == 0 %}
                            <tr name="hunter" class="hidden">
                                <th width="32" class="p-0 text-center">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="32" src="{{ asset('img/hunter.webp') }}">
                                </th>
                                <td class="text-center p-0">
                                    {{ 'ship.hunter'|trans|raw }} {{ 'ship.point'|trans|raw }} <span class="text-rouge">0</span>
                                </td>
                                <td class="p-0 text-center mt-1">
                                    {{ 'ship.armor'|trans|raw }} {{ commander.getShip().getArmorHunter() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.shield'|trans|raw }} 0
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.accurate'|trans|raw }} {{ commander.getShip().getAccurateHunter() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.missile'|trans|raw }} {{ commander.getShip().getMissileHunter() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.laser'|trans|raw }} 0
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.plasma'|trans|raw }} 0
                                </td>
                            </tr>
                        {% else %}
                            {% set form_shipPerso = formObject.createView %}
                            {{ form_start(form_shipPerso) }}
                            <tr name="hunter" class="hidden">
                                <th width="74" rowspan="2" class="p-0">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/hunter.webp') }}">
                                </th>
                                <td rowspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.ship,{'attr': { 'hidden': 'hidden', 'value': 'hunter' }}) }}
                                    {{ 'ship.hunter'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="totalPoints">{{ commander.getShip().getPointHunter() }}</span><span class="totalExec hidden">{{ commander.getShip().getPointHunter() }}</span>
                                </td>
                                <td>
                                    {{ 'ship.armor'|trans|raw }} <span class="addPoint">{{ commander.getShip().getArmorHunter() }}</span> <span class="basis" hidden>{{ commander.getShip().getArmorHunter() }}</span>
                                </td>
                                <td class="center-cells">
                                    {% if app.user.getTutorial() == 19 %}
                                        {{ form_widget(form_shipPerso.armor,{attr: { class: 'game-input tutorial-show' }}) }}
                                    {% else %}
                                        {{ form_widget(form_shipPerso.armor) }}
                                    {% endif %}
                                </td>
                                <td>
                                    {{ 'ship.shield'|trans|raw }} 0
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.shield,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                                <td>
                                    {{ 'ship.accurate'|trans|raw }} <span class="addPoint">{{ commander.getShip().getAccurateHunter() }}</span> <span class="basis" hidden>{{ commander.getShip().getAccurateHunter() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.accurate,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="hunter" class="hidden">
                                <td>
                                    {{ 'ship.missile'|trans|raw }} <span class="addPoint">{{ commander.getShip().getMissileHunter() }}</span> <span class="basis" hidden>{{ commander.getShip().getMissileHunter() }}</span>
                                </td>
                                <td class="center-cells">
                                    {% if app.user.getTutorial() == 19 %}
                                        {{ form_widget(form_shipPerso.missile,{attr: { class: 'game-input tutorial-show' }}) }}
                                    {% else %}
                                        {{ form_widget(form_shipPerso.missile) }}
                                    {% endif %}
                                </td>
                                <td>
                                    {{ 'ship.laser'|trans|raw }} 0
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.laser,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                                <td>
                                    {{ 'ship.plasma'|trans|raw }} 0
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.plasma,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="hunter" class="hidden">
                                <td colspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.sendForm) }}
                                </td>
                            </tr>
                            {{ form_end(form_shipPerso) }}
                        {% endif %}
                        {% if commander.getShip().getPointHunterHeavy() == 0 %}
                            <tr name="hunterHeavy" class="hidden">
                                <th width="32" class="p-0 text-center">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="32" src="{{ asset('img/hunterHeavy.webp') }}">
                                </th>
                                <td class="text-center p-0">
                                    {{ 'ship.hunterHeavy'|trans|raw }} {{ 'ship.point'|trans|raw }} <span class="text-rouge">0</span>
                                </td>
                                <td class="p-0 text-center mt-1">
                                    {{ 'ship.armor'|trans|raw }} {{ commander.getShip().getArmorHunterHeavy() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.shield'|trans|raw }} 0
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.accurate'|trans|raw }} {{ commander.getShip().getAccurateHunterHeavy() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.missile'|trans|raw }} {{ commander.getShip().getMissileHunterHeavy() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.laser'|trans|raw }} 0
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.plasma'|trans|raw }} 0
                                </td>
                            </tr>
                        {% else %}
                            {% set form_shipPerso = formObject.createView %}
                            {{ form_start(form_shipPerso) }}
                            <tr name="hunterHeavy" class="hidden">
                                <th width="74" rowspan="2" class="p-0">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/hunterHeavy.webp') }}">
                                </th>
                                <td rowspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.ship,{'attr': { 'hidden': 'hidden', 'value': 'hunterHeavy' }}) }}
                                    {{ 'ship.hunterHeavy'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="totalPoints">{{ commander.getShip().getPointHunterHeavy() }}</span><span class="totalExec hidden">{{ commander.getShip().getPointHunterHeavy() }}</span>
                                </td>
                                <td>
                                    {{ 'ship.armor'|trans|raw }} <span class="addPoint">{{ commander.getShip().getArmorHunterHeavy() }}</span> <span class="basis" hidden>{{ commander.getShip().getArmorHunterHeavy() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.armor) }}
                                </td>
                                <td>
                                    {{ 'ship.shield'|trans|raw }} 0
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.shield,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                                <td>
                                    {{ 'ship.accurate'|trans|raw }} <span class="addPoint">{{ commander.getShip().getAccurateHunterHeavy() }}</span> <span class="basis" hidden>{{ commander.getShip().getAccurateHunterHeavy() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.accurate,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="hunterHeavy" class="hidden">
                                <td>
                                    {{ 'ship.missile'|trans|raw }} <span class="addPoint">{{ commander.getShip().getMissileHunterHeavy() }}</span> <span class="basis" hidden>{{ commander.getShip().getMissileHunterHeavy() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.missile) }}
                                </td>
                                <td>
                                    {{ 'ship.laser'|trans|raw }} 0
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.laser,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                                <td>
                                    {{ 'ship.plasma'|trans|raw }} 0
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.plasma,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="hunterHeavy" class="hidden">
                                <td colspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.sendForm) }}
                                </td>
                            </tr>
                            {{ form_end(form_shipPerso) }}
                        {% endif %}
                        {% if commander.getShip().getPointHunterWar() == 0 %}
                            <tr name="hunterWar" class="hidden">
                                <th width="32" class="p-0 text-center">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="32" src="{{ asset('img/hunterWar.webp') }}">
                                </th>
                                <td class="text-center p-0">
                                    {{ 'ship.hunterWar'|trans|raw }} {{ 'ship.point'|trans|raw }} <span class="text-rouge">0</span>
                                </td>
                                <td class="p-0 text-center mt-1">
                                    {{ 'ship.armor'|trans|raw }} {{ commander.getShip().getArmorHunterWar() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.shield'|trans|raw }} 0
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.accurate'|trans|raw }} {{ commander.getShip().getAccurateHunterWar() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.missile'|trans|raw }} {{ commander.getShip().getMissileHunterWar() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.laser'|trans|raw }} {{ commander.getShip().getLaserHunterWar() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.plasma'|trans|raw }}  {{ commander.getShip().getPlasmaHunterWar() }}
                                </td>
                            </tr>
                        {% else %}
                            {% set form_shipPerso = formObject.createView %}
                            {{ form_start(form_shipPerso) }}
                            <tr name="hunterWar" class="hidden">
                                <th width="74" rowspan="2" class="p-0">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/hunterWar.webp') }}">
                                </th>
                                <td rowspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.ship,{'attr': { 'hidden': 'hidden', 'value': 'hunterWar' }}) }}
                                    {{ 'ship.hunterWar'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="totalPoints">{{ commander.getShip().getPointHunterWar() }}</span><span class="totalExec hidden">{{ commander.getShip().getPointHunterWar() }}</span>
                                </td>
                                <td>
                                    {{ 'ship.armor'|trans|raw }} <span class="addPoint">{{ commander.getShip().getArmorHunterWar() }}</span> <span class="basis" hidden>{{ commander.getShip().getArmorHunterWar() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.armor) }}
                                </td>
                                <td>
                                    {{ 'ship.shield'|trans|raw }} 0
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.shield,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                                <td>
                                    {{ 'ship.accurate'|trans|raw }} <span class="addPoint">{{ commander.getShip().getAccurateHunterWar() }}</span> <span class="basis" hidden>{{ commander.getShip().getAccurateHunterWar() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.accurate,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="hunterWar" class="hidden">
                                <td>
                                    {{ 'ship.missile'|trans|raw }} <span class="addPoint">{{ commander.getShip().getMissileHunterWar() }}</span> <span class="basis" hidden>{{ commander.getShip().getMissileHunterWar() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.missile) }}
                                </td>
                                <td>
                                    {{ 'ship.laser'|trans|raw }} <span class="addPoint">{{ commander.getShip().getLaserHunterWar() }}</span> <span class="basis" hidden>{{ commander.getShip().getLaserHunterWar() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.laser) }}
                                </td>
                                <td>
                                    {{ 'ship.plasma'|trans|raw }} <span class="addPoint">{{ commander.getShip().getPlasmaHunterWar() }}</span> <span class="basis" hidden>{{ commander.getShip().getPlasmaHunterWar() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.plasma) }}
                                </td>
                            </tr>
                            <tr name="hunterWar" class="hidden">
                                <td colspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.sendForm) }}
                                </td>
                            </tr>
                            {{ form_end(form_shipPerso) }}
                        {% endif %}
                        {% if commander.getShip().getPointCorvet() == 0 %}
                            <tr name="corvet" class="hidden">
                                <th width="32" class="p-0 text-center">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="32" src="{{ asset('img/corvet.webp') }}">
                                </th>
                                <td class="text-center p-0">
                                    {{ 'ship.corvet'|trans|raw }} {{ 'ship.point'|trans|raw }} <span class="text-rouge">0</span>
                                </td>
                                <td class="p-0 text-center mt-1">
                                    {{ 'ship.armor'|trans|raw }} {{ commander.getShip().getArmorCorvet() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.shield'|trans|raw }} {{ commander.getShip().getShieldCorvet() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.accurate'|trans|raw }} {{ commander.getShip().getAccurateCorvet() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.missile'|trans|raw }} {{ commander.getShip().getMissileCorvet() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.laser'|trans|raw }} 0
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.plasma'|trans|raw }} 0
                                </td>
                            </tr>
                        {% else %}
                            {% set form_shipPerso = formObject.createView %}
                            {{ form_start(form_shipPerso) }}
                            <tr name="corvet" class="hidden">
                                <th width="74" rowspan="2" class="p-0">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/corvet.webp') }}">
                                </th>
                                <td rowspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.ship,{'attr': { 'hidden': 'hidden', 'value': 'corvet' }}) }}
                                    {{ 'ship.corvet'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="totalPoints">{{ commander.getShip().getPointCorvet() }}</span><span class="totalExec hidden">{{ commander.getShip().getPointCorvet() }}</span>
                                </td>
                                <td>
                                    {{ 'ship.armor'|trans|raw }} <span class="addPoint">{{ commander.getShip().getArmorCorvet() }}</span> <span class="basis" hidden>{{ commander.getShip().getArmorCorvet() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.armor) }}
                                </td>
                                <td>
                                    {{ 'ship.shield'|trans|raw }} <span class="addPoint">{{ commander.getShip().getShieldCorvet() }}</span> <span class="basis" hidden>{{ commander.getShip().getShieldCorvet() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.shield) }}
                                </td>
                                <td>
                                    {{ 'ship.accurate'|trans|raw }} <span class="addPoint">{{ commander.getShip().getAccurateCorvet() }}</span> <span class="basis" hidden>{{ commander.getShip().getAccurateCorvet() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.accurate,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="corvet" class="hidden">
                                <td>
                                    {{ 'ship.missile'|trans|raw }} <span class="addPoint">{{ commander.getShip().getMissileCorvet() }}</span> <span class="basis" hidden>{{ commander.getShip().getMissileCorvet() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.missile) }}
                                </td>
                                <td>
                                    {{ 'ship.laser'|trans|raw }} 0
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.laser,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                                <td>
                                    {{ 'ship.plasma'|trans|raw }} 0
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.plasma,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="corvet" class="hidden">
                                <td colspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.sendForm) }}
                                </td>
                            </tr>
                            {{ form_end(form_shipPerso) }}
                        {% endif %}
                        {% if commander.getShip().getPointCorvetLaser() == 0 %}
                            <tr name="corvetLaser" class="hidden">
                                <th width="32" class="p-0 text-center">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="32" src="{{ asset('img/corvetLaser.webp') }}">
                                </th>
                                <td class="text-center p-0">
                                    {{ 'ship.corvetLaser'|trans|raw }} {{ 'ship.point'|trans|raw }} <span class="text-rouge">0</span>
                                </td>
                                <td class="p-0 text-center mt-1">
                                    {{ 'ship.armor'|trans|raw }} {{ commander.getShip().getArmorCorvetLaser() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.shield'|trans|raw }} {{ commander.getShip().getShieldCorvetLaser() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.accurate'|trans|raw }} {{ commander.getShip().getAccurateCorvetLaser() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.missile'|trans|raw }} {{ commander.getShip().getMissileCorvetLaser() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.laser'|trans|raw }} {{ commander.getShip().getLaserCorvetLaser() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.plasma'|trans|raw }}  0
                                </td>
                            </tr>
                        {% else %}
                            {% set form_shipPerso = formObject.createView %}
                            {{ form_start(form_shipPerso) }}
                            <tr name="corvetLaser" class="hidden">
                                <th width="74" rowspan="2" class="p-0">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/corvetLaser.webp') }}">
                                </th>
                                <td rowspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.ship,{'attr': { 'hidden': 'hidden', 'value': 'corvetLaser' }}) }}
                                    {{ 'ship.corvetLaser'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="totalPoints">{{ commander.getShip().getPointCorvetLaser() }}</span><span class="totalExec hidden">{{ commander.getShip().getPointCorvetLaser() }}</span>
                                </td>
                                <td>
                                    {{ 'ship.armor'|trans|raw }} <span class="addPoint">{{ commander.getShip().getArmorCorvetLaser() }}</span> <span class="basis" hidden>{{ commander.getShip().getArmorCorvetLaser() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.armor) }}
                                </td>
                                <td>
                                    {{ 'ship.shield'|trans|raw }} <span class="addPoint">{{ commander.getShip().getShieldCorvetLaser() }}</span> <span class="basis" hidden>{{ commander.getShip().getShieldCorvetLaser() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.shield) }}
                                </td>
                                <td>
                                    {{ 'ship.accurate'|trans|raw }} <span class="addPoint">{{ commander.getShip().getAccurateCorvetLaser() }}</span> <span class="basis" hidden>{{ commander.getShip().getAccurateCorvetLaser() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.accurate,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="corvetLaser" class="hidden">
                                <td>
                                    {{ 'ship.missile'|trans|raw }} <span class="addPoint">{{ commander.getShip().getMissileCorvetLaser() }}</span> <span class="basis" hidden>{{ commander.getShip().getMissileCorvetLaser() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.missile) }}
                                </td>
                                <td>
                                    {{ 'ship.laser'|trans|raw }} <span class="addPoint">{{ commander.getShip().getLaserCorvetLaser() }}</span> <span class="basis" hidden>{{ commander.getShip().getLaserCorvetLaser() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.laser) }}
                                </td>
                                <td>
                                    {{ 'ship.plasma'|trans|raw }} 0
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.plasma,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="corvetLaser" class="hidden">
                                <td colspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.sendForm) }}
                                </td>
                            </tr>
                            {{ form_end(form_shipPerso) }}
                        {% endif %}
                        {% if commander.getShip().getPointCorvetWar() == 0 %}
                            <tr name="corvetWar" class="hidden">
                                <th width="32" class="p-0 text-center">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="32" src="{{ asset('img/corvetWar.webp') }}">
                                </th>
                                <td class="text-center p-0">
                                    {{ 'ship.corvetWar'|trans|raw }} {{ 'ship.point'|trans|raw }} <span class="text-rouge">0</span>
                                </td>
                                <td class="p-0 text-center mt-1">
                                    {{ 'ship.armor'|trans|raw }} {{ commander.getShip().getArmorCorvetWar() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.shield'|trans|raw }} {{ commander.getShip().getShieldCorvetWar() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.accurate'|trans|raw }} {{ commander.getShip().getAccurateCorvetWar() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.missile'|trans|raw }} {{ commander.getShip().getMissileCorvetWar() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.laser'|trans|raw }} {{ commander.getShip().getLaserCorvetWar() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.plasma'|trans|raw }}
                                </td>
                            </tr>
                        {% else %}
                            {% set form_shipPerso = formObject.createView %}
                            {{ form_start(form_shipPerso) }}
                            <tr name="corvetWar" class="hidden">
                                <th width="74" rowspan="2" class="p-0">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/corvetWar.webp') }}">
                                </th>
                                <td rowspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.ship,{'attr': { 'hidden': 'hidden', 'value': 'corvetWar' }}) }}
                                    {{ 'ship.corvetWar'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="totalPoints">{{ commander.getShip().getPointCorvetWar() }}</span><span class="totalExec hidden">{{ commander.getShip().getPointCorvetWar() }}</span>
                                </td>
                                <td>
                                    {{ 'ship.armor'|trans|raw }} <span class="addPoint">{{ commander.getShip().getArmorCorvetWar() }}</span> <span class="basis" hidden>{{ commander.getShip().getArmorCorvetWar() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.armor) }}
                                </td>
                                <td>
                                    {{ 'ship.shield'|trans|raw }} <span class="addPoint">{{ commander.getShip().getShieldCorvetWar() }}</span> <span class="basis" hidden>{{ commander.getShip().getShieldCorvetWar() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.shield) }}
                                </td>
                                <td>
                                    {{ 'ship.accurate'|trans|raw }} <span class="addPoint">{{ commander.getShip().getAccurateCorvetWar() }}</span> <span class="basis" hidden>{{ commander.getShip().getAccurateCorvetWar() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.accurate,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="corvetWar" class="hidden">
                                <td>
                                    {{ 'ship.missile'|trans|raw }} <span class="addPoint">{{ commander.getShip().getMissileCorvetWar() }}</span> <span class="basis" hidden>{{ commander.getShip().getMissileCorvetWar() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.missile) }}
                                </td>
                                <td>
                                    {{ 'ship.laser'|trans|raw }}  <span class="addPoint">{{ commander.getShip().getLaserCorvetWar() }}</span> <span class="basis" hidden>{{ commander.getShip().getLaserCorvetWar() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.laser) }}
                                </td>
                                <td>
                                    {{ 'ship.plasma'|trans|raw }} 0
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.plasma,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="corvetWar" class="hidden">
                                <td colspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.sendForm) }}
                                </td>
                            </tr>
                            {{ form_end(form_shipPerso) }}
                        {% endif %}
                        {% if commander.getShip().getPointFregate() == 0 %}
                            <tr name="fregate" class="hidden">
                                <th width="32" class="p-0 text-center">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="32" src="{{ asset('img/fregate.webp') }}">
                                </th>
                                <td class="text-center p-0">
                                    {{ 'ship.fregate'|trans|raw }} {{ 'ship.point'|trans|raw }} <span class="text-rouge">0</span>
                                </td>
                                <td class="p-0 text-center mt-1">
                                    {{ 'ship.armor'|trans|raw }} {{ commander.getShip().getArmorFregate() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.shield'|trans|raw }} {{ commander.getShip().getShieldFregate() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.accurate'|trans|raw }} {{ commander.getShip().getAccurateFregate() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.missile'|trans|raw }} {{ commander.getShip().getMissileFregate() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.laser'|trans|raw }} {{ commander.getShip().getLaserFregate() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.plasma'|trans|raw }}
                                </td>
                            </tr>
                        {% else %}
                            {% set form_shipPerso = formObject.createView %}
                            {{ form_start(form_shipPerso) }}
                            <tr name="fregate" class="hidden">
                                <th width="74" rowspan="2" class="p-0">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/fregate.webp') }}">
                                </th>
                                <td rowspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.ship,{'attr': { 'hidden': 'hidden', 'value': 'fregate' }}) }}
                                    {{ 'ship.fregate'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="totalPoints">{{ commander.getShip().getPointFregate() }}</span><span class="totalExec hidden">{{ commander.getShip().getPointFregate() }}</span>
                                </td>
                                <td>
                                    {{ 'ship.armor'|trans|raw }} <span class="addPoint">{{ commander.getShip().getArmorFregate() }}</span> <span class="basis" hidden>{{ commander.getShip().getArmorFregate() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.armor) }}
                                </td>
                                <td>
                                    {{ 'ship.shield'|trans|raw }} <span class="addPoint">{{ commander.getShip().getShieldFregate() }}</span> <span class="basis" hidden>{{ commander.getShip().getShieldFregate() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.shield) }}
                                </td>
                                <td>
                                    {{ 'ship.accurate'|trans|raw }} <span class="addPoint">{{ commander.getShip().getAccurateFregate() }}</span> <span class="basis" hidden>{{ commander.getShip().getAccurateFregate() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.accurate,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="fregate" class="hidden">
                                <td>
                                    {{ 'ship.missile'|trans|raw }} <span class="addPoint">{{ commander.getShip().getMissileFregate() }}</span> <span class="basis" hidden>{{ commander.getShip().getMissileFregate() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.missile) }}
                                </td>
                                <td>
                                    {{ 'ship.laser'|trans|raw }}  <span class="addPoint">{{ commander.getShip().getLaserFregate() }}</span> <span class="basis" hidden>{{ commander.getShip().getLaserFregate() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.laser) }}
                                </td>
                                <td>
                                    {{ 'ship.plasma'|trans|raw }} 0
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.plasma,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="fregate" class="hidden">
                                <td colspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.sendForm) }}
                                </td>
                            </tr>
                            {{ form_end(form_shipPerso) }}
                        {% endif %}
                        {% if commander.getShip().getPointFregatePlasma() == 0 %}
                            <tr name="fregatePlasma" class="hidden">
                                <th width="32" class="p-0 text-center">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="32" src="{{ asset('img/fregatePlasma.webp') }}">
                                </th>
                                <td class="text-center p-0">
                                    {{ 'ship.fregatePlasma'|trans|raw }} {{ 'ship.point'|trans|raw }} <span class="text-rouge">0</span>
                                </td>
                                <td class="p-0 text-center mt-1">
                                    {{ 'ship.armor'|trans|raw }} {{ commander.getShip().getArmorFregatePlasma() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.shield'|trans|raw }} {{ commander.getShip().getShieldFregatePlasma() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.accurate'|trans|raw }} {{ commander.getShip().getAccurateFregatePlasma() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.missile'|trans|raw }} {{ commander.getShip().getMissileFregatePlasma() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.laser'|trans|raw }} {{ commander.getShip().getLaserFregatePlasma() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.plasma'|trans|raw }} {{ commander.getShip().getPlasmaFregatePlasma() }}
                                </td>
                            </tr>
                        {% else %}
                            {% set form_shipPerso = formObject.createView %}
                            {{ form_start(form_shipPerso) }}
                            <tr name="fregatePlasma" class="hidden">
                                <th width="74" rowspan="2" class="p-0">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/fregatePlasma.webp') }}">
                                </th>
                                <td rowspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.ship,{'attr': { 'hidden': 'hidden', 'value': 'fregatePlasma' }}) }}
                                    {{ 'ship.fregatePlasma'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="totalPoints">{{ commander.getShip().getPointFregatePlasma() }}</span><span class="totalExec hidden">{{ commander.getShip().getPointFregatePlasma() }}</span>
                                </td>
                                <td>
                                    {{ 'ship.armor'|trans|raw }} <span class="addPoint">{{ commander.getShip().getArmorFregatePlasma() }}</span> <span class="basis" hidden>{{ commander.getShip().getArmorFregatePlasma() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.armor) }}
                                </td>
                                <td>
                                    {{ 'ship.shield'|trans|raw }} <span class="addPoint">{{ commander.getShip().getShieldFregatePlasma() }}</span> <span class="basis" hidden>{{ commander.getShip().getShieldFregatePlasma() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.shield) }}
                                </td>
                                <td>
                                    {{ 'ship.accurate'|trans|raw }} <span class="addPoint">{{ commander.getShip().getAccurateFregatePlasma() }}</span> <span class="basis" hidden>{{ commander.getShip().getAccurateFregatePlasma() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.accurate,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="fregatePlasma" class="hidden">
                                <td>
                                    {{ 'ship.missile'|trans|raw }} <span class="addPoint">{{ commander.getShip().getMissileFregatePlasma() }}</span> <span class="basis" hidden>{{ commander.getShip().getMissileFregatePlasma() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.missile) }}
                                </td>
                                <td>
                                    {{ 'ship.laser'|trans|raw }} <span class="addPoint">{{ commander.getShip().getLaserFregatePlasma() }}</span> <span class="basis" hidden>{{ commander.getShip().getLaserFregatePlasma() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.laser) }}
                                </td>
                                <td>
                                    {{ 'ship.plasma'|trans|raw }} <span class="addPoint">{{ commander.getShip().getPlasmaFregatePlasma() }}</span> <span class="basis" hidden>{{ commander.getShip().getPlasmaFregatePlasma() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.plasma) }}
                                </td>
                            </tr>
                            <tr name="fregatePlasma" class="hidden">
                                <td colspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.sendForm) }}
                                </td>
                            </tr>
                            {{ form_end(form_shipPerso) }}
                        {% endif %}
                        {% if commander.getShip().getPointCroiser() == 0 %}
                            <tr name="croiser" class="hidden">
                                <th width="32" class="p-0 text-center">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="32" src="{{ asset('img/croiser.webp') }}">
                                </th>
                                <td class="text-center p-0">
                                    {{ 'ship.croiser'|trans|raw }} {{ 'ship.point'|trans|raw }} <span class="text-rouge">0</span>
                                </td>
                                <td class="p-0 text-center mt-1">
                                    {{ 'ship.armor'|trans|raw }} {{ commander.getShip().getArmorCroiser() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.shield'|trans|raw }} {{ commander.getShip().getShieldCroiser() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.accurate'|trans|raw }} {{ commander.getShip().getAccurateCroiser() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.missile'|trans|raw }} {{ commander.getShip().getMissileCroiser() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.laser'|trans|raw }} {{ commander.getShip().getLaserCroiser() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.plasma'|trans|raw }} {{ commander.getShip().getPlasmaCroiser() }}
                                </td>
                            </tr>
                        {% else %}
                            {% set form_shipPerso = formObject.createView %}
                            {{ form_start(form_shipPerso) }}
                            <tr name="croiser" class="hidden">
                                <th width="74" rowspan="2" class="p-0">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/croiser.webp') }}">
                                </th>
                                <td rowspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.ship,{'attr': { 'hidden': 'hidden', 'value': 'croiser' }}) }}
                                    {{ 'ship.croiser'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="totalPoints">{{ commander.getShip().getPointCroiser() }}</span><span class="totalExec hidden">{{ commander.getShip().getPointCroiser() }}</span>
                                </td>
                                <td>
                                    {{ 'ship.armor'|trans|raw }} <span class="addPoint">{{ commander.getShip().getArmorCroiser() }}</span> <span class="basis" hidden>{{ commander.getShip().getArmorCroiser() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.armor) }}
                                </td>
                                <td>
                                    {{ 'ship.shield'|trans|raw }} <span class="addPoint">{{ commander.getShip().getShieldCroiser() }}</span> <span class="basis" hidden>{{ commander.getShip().getShieldCroiser() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.shield) }}
                                </td>
                                <td>
                                    {{ 'ship.accurate'|trans|raw }} <span class="addPoint">{{ commander.getShip().getAccurateCroiser() }}</span> <span class="basis" hidden>{{ commander.getShip().getAccurateCroiser() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.accurate,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="croiser" class="hidden">
                                <td>
                                    {{ 'ship.missile'|trans|raw }} <span class="addPoint">{{ commander.getShip().getMissileCroiser() }}</span> <span class="basis" hidden>{{ commander.getShip().getMissileCroiser() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.missile) }}
                                </td>
                                <td>
                                    {{ 'ship.laser'|trans|raw }} <span class="addPoint">{{ commander.getShip().getLaserCroiser() }}</span> <span class="basis" hidden>{{ commander.getShip().getLaserCroiser() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.laser) }}
                                </td>
                                <td>
                                    {{ 'ship.plasma'|trans|raw }} <span class="addPoint">{{ commander.getShip().getPlasmaCroiser() }}</span> <span class="basis" hidden>{{ commander.getShip().getPlasmaCroiser() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.plasma) }}
                                </td>
                            </tr>
                            <tr name="croiser" class="hidden">
                                <td colspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.sendForm) }}
                                </td>
                            </tr>
                            {{ form_end(form_shipPerso) }}
                        {% endif %}
                        {% if commander.getShip().getPointIronClad() == 0 %}
                            <tr name="ironClad" class="hidden">
                                <th width="32" class="p-0 text-center">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="32" src="{{ asset('img/ironClad.webp') }}">
                                </th>
                                <td class="text-center p-0">
                                    {{ 'ship.ironClad'|trans|raw }} {{ 'ship.point'|trans|raw }} <span class="text-rouge">0</span>
                                </td>
                                <td class="p-0 text-center mt-1">
                                    {{ 'ship.armor'|trans|raw }} {{ commander.getShip().getArmorIronClad() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.shield'|trans|raw }} {{ commander.getShip().getShieldIronClad() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.accurate'|trans|raw }} {{ commander.getShip().getAccurateIronClad() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.missile'|trans|raw }} {{ commander.getShip().getMissileIronClad() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.laser'|trans|raw }} {{ commander.getShip().getLaserIronClad() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.plasma'|trans|raw }} {{ commander.getShip().getPlasmaIronClad() }}
                                </td>
                            </tr>
                        {% else %}
                            {% set form_shipPerso = formObject.createView %}
                            {{ form_start(form_shipPerso) }}
                            <tr name="ironClad" class="hidden">
                                <th width="74" rowspan="2" class="p-0">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/ironClad.webp') }}">
                                </th>
                                <td rowspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.ship,{'attr': { 'hidden': 'hidden', 'value': 'ironClad' }}) }}
                                    {{ 'ship.ironClad'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="totalPoints">{{ commander.getShip().getPointIronClad() }}</span><span class="totalExec hidden">{{ commander.getShip().getPointIronClad() }}</span>
                                </td>
                                <td>
                                    {{ 'ship.armor'|trans|raw }} <span class="addPoint">{{ commander.getShip().getArmorIronClad() }}</span> <span class="basis" hidden>{{ commander.getShip().getArmorIronClad() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.armor) }}
                                </td>
                                <td>
                                    {{ 'ship.shield'|trans|raw }} <span class="addPoint">{{ commander.getShip().getShieldIronClad() }}</span> <span class="basis" hidden>{{ commander.getShip().getShieldIronClad() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.shield) }}
                                </td>
                                <td>
                                    {{ 'ship.accurate'|trans|raw }} <span class="addPoint">{{ commander.getShip().getAccurateIronClad() }}</span> <span class="basis" hidden>{{ commander.getShip().getAccurateIronClad() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.accurate,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="ironClad" class="hidden">
                                <td>
                                    {{ 'ship.missile'|trans|raw }} <span class="addPoint">{{ commander.getShip().getMissileIronClad() }}</span> <span class="basis" hidden>{{ commander.getShip().getMissileIronClad() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.missile) }}
                                </td>
                                <td>
                                    {{ 'ship.laser'|trans|raw }} <span class="addPoint">{{ commander.getShip().getLaserIronClad() }}</span> <span class="basis" hidden>{{ commander.getShip().getLaserIronClad() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.laser) }}
                                </td>
                                <td>
                                    {{ 'ship.plasma'|trans|raw }} <span class="addPoint">{{ commander.getShip().getPlasmaIronClad() }}</span> <span class="basis" hidden>{{ commander.getShip().getPlasmaIronClad() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.plasma) }}
                                </td>
                            </tr>
                            <tr name="ironClad" class="hidden">
                                <td colspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.sendForm) }}
                                </td>
                            </tr>
                            {{ form_end(form_shipPerso) }}
                        {% endif %}
                        {% if commander.getShip().getPointDestroyer() == 0 %}
                            <tr name="destroyer" class="hidden">
                                <th width="32" class="p-0 text-center">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="32" src="{{ asset('img/destroyer.webp') }}">
                                </th>
                                <td class="text-center p-0">
                                    {{ 'ship.destroyer'|trans|raw }} {{ 'ship.point'|trans|raw }} <span class="text-rouge">0</span>
                                </td>
                                <td class="p-0 text-center mt-1">
                                    {{ 'ship.armor'|trans|raw }} {{ commander.getShip().getArmorDestroyer() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.shield'|trans|raw }} {{ commander.getShip().getShieldDestroyer() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.accurate'|trans|raw }} {{ commander.getShip().getAccurateDestroyer() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.missile'|trans|raw }} {{ commander.getShip().getMissileDestroyer() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.laser'|trans|raw }} {{ commander.getShip().getLaserDestroyer() }}
                                </td>
                                <td class="p-0 text-center">
                                    {{ 'ship.plasma'|trans|raw }} {{ commander.getShip().getPlasmaDestroyer() }}
                                </td>
                            </tr>
                        {% else %}
                            {% set form_shipPerso = formObject.createView %}
                            {{ form_start(form_shipPerso) }}
                            <tr name="destroyer" class="hidden">
                                <th width="74" rowspan="2" class="p-0">
                                    <img alt="{{ 'image'|trans({},'front_global') }}" width="74" src="{{ asset('img/destroyer.webp') }}">
                                </th>
                                <td rowspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.ship,{'attr': { 'hidden': 'hidden', 'value': 'destroyer' }}) }}
                                    {{ 'ship.destroyer'|trans|raw }}<br> {{ 'ship.point'|trans|raw }} <span class="totalPoints">{{ commander.getShip().getPointDestroyer() }}</span><span class="totalExec hidden">{{ commander.getShip().getPointDestroyer() }}</span>
                                </td>
                                <td>
                                    {{ 'ship.armor'|trans|raw }} <span class="addPoint">{{ commander.getShip().getArmorDestroyer() }}</span> <span class="basis" hidden>{{ commander.getShip().getArmorDestroyer() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.armor) }}
                                </td>
                                <td>
                                    {{ 'ship.shield'|trans|raw }} <span class="addPoint">{{ commander.getShip().getShieldDestroyer() }}</span> <span class="basis" hidden>{{ commander.getShip().getShieldDestroyer() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.shield) }}
                                </td>
                                <td>
                                    {{ 'ship.accurate'|trans|raw }} <span class="addPoint">{{ commander.getShip().getAccurateDestroyer() }}</span> <span class="basis" hidden>{{ commander.getShip().getAccurateDestroyer() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.accurate,{'attr': { 'disabled': 'disabled', 'class': 'game-input-off text-left' }}) }}
                                </td>
                            </tr>
                            <tr name="destroyer" class="hidden">
                                <td>
                                    {{ 'ship.missile'|trans|raw }} <span class="addPoint">{{ commander.getShip().getMissileDestroyer() }}</span> <span class="basis" hidden>{{ commander.getShip().getMissileDestroyer() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.missile) }}
                                </td>
                                <td>
                                    {{ 'ship.laser'|trans|raw }} <span class="addPoint">{{ commander.getShip().getLaserDestroyer() }}</span> <span class="basis" hidden>{{ commander.getShip().getLaserDestroyer() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.laser) }}
                                </td>
                                <td>
                                    {{ 'ship.plasma'|trans|raw }} <span class="addPoint">{{ commander.getShip().getPlasmaDestroyer() }}</span> <span class="basis" hidden>{{ commander.getShip().getPlasmaDestroyer() }}</span>
                                </td>
                                <td class="center-cells">
                                    {{ form_widget(form_shipPerso.plasma) }}
                                </td>
                            </tr>
                            <tr name="destroyer" class="hidden">
                                <td colspan="2" class="text-center">
                                    {{ form_widget(form_shipPerso.sendForm) }}
                                </td>
                            </tr>
                            {{ form_end(form_shipPerso) }}
                        {% endif %}
                        </tbody>
                    </table>
                    {% if commander.getShip().getRemainingPoints() > 0 %}{% if commander.getShip().getMax() > 0  %}<div class="block-title mt-4"><h2>{{ 'ship.lastPoints'|trans({'%nbr%': commander.getShip().getMax()}) }}</h2></div>{% else %}<div class="block-title mt-4"><h2>{{ 'ship.nextUpdate'|trans }} <span class="timerArea" hidden>{{ commander.getShip().getLastUpdate()|date('Y/m/d H:i:s') }}</span></h2></div>{% endif %}{% endif %}
                </div>
            </div>
            {% if commander.getShip().getRetry() > 0  %}<span><a class="btn btn-lg btn-start float-right mr-2 mb-2{% if app.user.getTutorial() < 20 %} tutorial-hide{% endif %}" data-toggle="tooltip" data-placement="top" title="{{ 'tooltip.shipRetry'|trans }}" href="{{ path('ship_retry', {'usePlanet': usePlanet.getId()}) }}"><span>{{ 'ship.retry'|trans({'%retry%': commander.getShip().getRetry()}) }}</a></span>{% endif %}
            <canvas class="mt-5" id="doughnutChart" height="150"></canvas>
            {{ encore_entry_script_tags('shipCharts') }}
        </div>
    </div>
{% endblock %}
