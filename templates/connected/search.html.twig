{% extends 'base.html.twig' %}

{% trans_default_domain 'front_search' %}

{% block title %}{{ 'title'|trans }}{% endblock %}

{% block body %}
    <div class="front-block research">
        <div class="block-title"><h2>{{ 'research.titleUtility'|trans }}</h2></div>
        <table class="table table-striped table-bordered table-dark text-center">
            <tbody>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.onde'|trans|raw }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('home', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.terraformation'|trans|raw }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('home', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.cargo'|trans|raw }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('home', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.recycleur'|trans|raw }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('home', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.utility'|trans|raw }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('home', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.hyperespace'|trans|raw }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('home', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
        <div class="block-title"><h2>{{ 'research.titleEco'|trans }}</h2></div>
        <table class="table table-striped table-bordered table-dark text-center">
            <tbody>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.demography'|trans|raw }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('home', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.discipline'|trans|raw }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('home', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
        <div class="block-title"><h2>{{ 'research.titleArm'|trans }}</h2></div>
        <table class="table table-striped table-bordered table-dark text-center">
            <tbody>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.armement'|trans|raw }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('home', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.missile'|trans|raw }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('home', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.laser'|trans|raw }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('home', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.plasma'|trans|raw }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('home', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
        <div class="block-title"><h2>{{ 'research.titleWar'|trans }}</h2></div>
        <table class="table table-striped table-bordered table-dark text-center">
            <tbody>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.industry'|trans|raw }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('home', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.lightShip'|trans|raw }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('home', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.heavyShip'|trans|raw }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('home', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
    </div>
{% endblock %}
