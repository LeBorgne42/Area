{% extends 'base.html.twig' %}

{% trans_default_domain 'front_search' %}

{% block title %}{{ 'title'|trans }}{% endblock %}

{% block body %}
    <div class="front-block research">
        <div class="block-title"><h2>{{ 'research.titleUtility'|trans }}</h2></div>
        <table class="table table-striped table-bordered table-dark text-center">
            <tbody>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.onde'|trans|raw }} x {{ app.user.getResearch().getOnde().getLevel() }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('research_onde', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.terraformation'|trans|raw }} {% if app.user.getResearch().getTerraformation().getLevel() == false %}{{ 'research.canBe'|trans }}{% else %}{{ 'research.Activated'|trans }}{% endif %}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('research_terraformation', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.cargo'|trans|raw }} x {{ app.user.getResearch().getCargo().getLevel() }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('research_cargo', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.recycleur'|trans|raw }} {% if app.user.getResearch().getRecycleur().getLevel() == false %}{{ 'research.canBe'|trans }}{% else %}{{ 'research.Activated'|trans }}{% endif %}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('research_recyclage', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.utility'|trans|raw }} x {{ app.user.getResearch().getUtility().getLevel() }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('research_utility', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.hyperespace'|trans|raw }} {% if app.user.getResearch().getHyperespace().getLevel() == false %}{{ 'research.canBe'|trans }}{% else %}{{ 'research.Activated'|trans }}{% endif %}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('research_hyperespace', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
        <div class="block-title"><h2>{{ 'research.titleEco'|trans }}</h2></div>
        <table class="table table-striped table-bordered table-dark text-center">
            <tbody>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.demography'|trans|raw }} x {{ app.user.getResearch().getDemography().getLevel() }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('research_demography', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.discipline'|trans|raw }} x {{ app.user.getResearch().getDiscipline().getLevel() }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('research_discipline', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
        <div class="block-title"><h2>{{ 'research.titleArm'|trans }}</h2></div>
        <table class="table table-striped table-bordered table-dark text-center">
            <tbody>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.armement'|trans|raw }} x {{ app.user.getResearch().getArmement().getLevel() }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('research_armement', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.missile'|trans|raw }} x {{ app.user.getResearch().getMissile().getLevel() }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('research_missile', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.laser'|trans|raw }} x {{ app.user.getResearch().getLaser().getLevel() }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('research_laser', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.plasma'|trans|raw }} x {{ app.user.getResearch().getPlasma().getLevel() }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('research_plasma', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
        <div class="block-title"><h2>{{ 'research.titleWar'|trans }}</h2></div>
        <table class="table table-striped table-bordered table-dark text-center">
            <tbody>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.industry'|trans|raw }} x {{ app.user.getResearch().getIndustry().getLevel() }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('research_industry', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.lightShip'|trans|raw }} x {{ app.user.getResearch().getLightShip().getLevel() }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('research_light_ship', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th class="tab-cells-name">
                    {{ 'title.heavyShip'|trans|raw }} x {{ app.user.getResearch().getHeavyShip().getLevel() }}
                </th>
                <td class="center-cells">
                    {% if app.user %}
                        <a class="btn btn-lg btn-info" href="{{ path('research_heavy_ship', {'idp': usePlanet.getId()}) }}">{{ 'research.seek'|trans }}</a>
                    {% else %}
                        <a class="btn btn-lg btn-info" disabled>{{ 'research.seek'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
    </div>
{% endblock %}
